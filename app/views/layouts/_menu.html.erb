<div class="container">    
  <div class="row mt-3">  
    <div class="justify-content-center">
      <table class="table">
        <td class="" >
          <%= link_to "Home", admins_path, class:"nav-link d-inline #{ "text-secondary" unless controller.controller_name == 'admin'}" %>
        </td>

        <td class="" >
          <%= link_to "Marcas", marcas_path, class:"nav-link active d-inline #{ "text-secondary" unless controller.controller_name == 'marcas'}" %>
        </td>

        <td class="" >
          <%= link_to "Veículos", veiculos_path, class:"nav-link active d-inline #{ "text-secondary" unless controller.controller_name == 'veiculos'}" %>
        </td>

        <td class="" >
          <%= link_to "Clientes", clientes_path, class:"nav-link active #{ "text-secondary" unless controller.controller_name == 'clientes'}" %>
        </td>

        <td class="" >
          <%= link_to "Administradores", admins_path, class:"nav-link active #{ "text-secondary" unless controller.controller_name == 'admins'}" %>
        </td>

        <td class="" >
          <%= link_to "Sair", logout_admin_path, class:"nav-link active #{ "text-secondary" unless controller.controller_name == 'admin'}" %>
        </td>
      </table>
      <hr class="fs-5">
    </div>
  </div>
</div>
